[package]
name = "how"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"

[dependencies]
clap = { version = "4.6", features = ["color", "derive"] }
owo-colors = { version = "4.3", features = ["supports-colors"] }
snafu = "0.9"
